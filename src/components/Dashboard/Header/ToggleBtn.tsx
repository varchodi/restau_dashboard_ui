import { Toggle_types } from '../../../common.types'

const ToggleBtn = ({icon,showBadge}:Toggle_types) => {
  return (
    <article>
          <p>{icon}</p>
    </article>
  )
}

export default ToggleBtn
